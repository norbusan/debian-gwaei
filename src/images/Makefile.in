# Makefile.in generated by automake 1.11.1 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008, 2009  Free Software Foundation,
# Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
subdir = src/images
DIST_COMMON = $(srcdir)/Makefile.am $(srcdir)/Makefile.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/gettext.m4 \
	$(top_srcdir)/m4/gnome-doc-utils.m4 $(top_srcdir)/m4/iconv.m4 \
	$(top_srcdir)/m4/lib-ld.m4 $(top_srcdir)/m4/lib-link.m4 \
	$(top_srcdir)/m4/lib-prefix.m4 $(top_srcdir)/m4/nls.m4 \
	$(top_srcdir)/m4/po.m4 $(top_srcdir)/m4/progtest.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__installdirs = "$(DESTDIR)$(data16dir)" "$(DESTDIR)$(data24dir)" \
	"$(DESTDIR)$(data32dir)" "$(DESTDIR)$(data48dir)" \
	"$(DESTDIR)$(datanonwordboundary16dir)" \
	"$(DESTDIR)$(datanonwordboundary22dir)" \
	"$(DESTDIR)$(datanonwordboundary24dir)" \
	"$(DESTDIR)$(dataunknowncharacter16dir)" \
	"$(DESTDIR)$(dataunknowncharacter22dir)" \
	"$(DESTDIR)$(dataunknowncharacter24dir)" \
	"$(DESTDIR)$(datawordboundary16dir)" \
	"$(DESTDIR)$(datawordboundary22dir)" \
	"$(DESTDIR)$(datawordboundary24dir)" "$(DESTDIR)$(pkgdatadir)"
DATA = $(data16_DATA) $(data24_DATA) $(data32_DATA) $(data48_DATA) \
	$(datanonwordboundary16_DATA) $(datanonwordboundary22_DATA) \
	$(datanonwordboundary24_DATA) $(dataunknowncharacter16_DATA) \
	$(dataunknowncharacter22_DATA) $(dataunknowncharacter24_DATA) \
	$(datawordboundary16_DATA) $(datawordboundary22_DATA) \
	$(datawordboundary24_DATA) $(pkgdata_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
COMMENT_FG_DEFAULT = @COMMENT_FG_DEFAULT@
CPPFLAGS = @CPPFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DEPS_CFLAGS = @DEPS_CFLAGS@
DEPS_LIBS = @DEPS_LIBS@
DISTCHECK_CONFIGURE_FLAGS = @DISTCHECK_CONFIGURE_FLAGS@
DOC_USER_FORMATS = @DOC_USER_FORMATS@
DPKG_DEB = @DPKG_DEB@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
ENGLISH_DEFAULT_URI = @ENGLISH_DEFAULT_URI@
EXAMPLES_DEFAULT_URI = @EXAMPLES_DEFAULT_URI@
EXEEXT = @EXEEXT@
FRENCH_DEFAULT_URI = @FRENCH_DEFAULT_URI@
GCONFTOOL = @GCONFTOOL@
GCONF_SCHEMA_CONFIG_SOURCE = @GCONF_SCHEMA_CONFIG_SOURCE@
GCONF_SCHEMA_FILE_DIR = @GCONF_SCHEMA_FILE_DIR@
GERMAN_DEFAULT_URI = @GERMAN_DEFAULT_URI@
GETTEXT_MACRO_VERSION = @GETTEXT_MACRO_VERSION@
GMSGFMT = @GMSGFMT@
GMSGFMT_015 = @GMSGFMT_015@
GNOME_CFLAGS = @GNOME_CFLAGS@
GNOME_LIBS = @GNOME_LIBS@
GUNZIP = @GUNZIP@
HEADER_BG_DEFAULT = @HEADER_BG_DEFAULT@
HEADER_FG_DEFAULT = @HEADER_FG_DEFAULT@
HELP_DIR = @HELP_DIR@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
INTLLIBS = @INTLLIBS@
INTL_MACOSX_LIBS = @INTL_MACOSX_LIBS@
KANJI_DEFAULT_URI = @KANJI_DEFAULT_URI@
KILLALL = @KILLALL@
LDFLAGS = @LDFLAGS@
LIBICONV = @LIBICONV@
LIBINTL = @LIBINTL@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIBSEXY_CFLAGS = @LIBSEXY_CFLAGS@
LIBSEXY_LIBS = @LIBSEXY_LIBS@
LIBUNIQUE_CFLAGS = @LIBUNIQUE_CFLAGS@
LIBUNIQUE_LIBS = @LIBUNIQUE_LIBS@
LN_S = @LN_S@
LOAD_ORDER_DEFAULT = @LOAD_ORDER_DEFAULT@
LTLIBICONV = @LTLIBICONV@
LTLIBINTL = @LTLIBINTL@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
MATCH_BG_DEFAULT = @MATCH_BG_DEFAULT@
MATCH_FG_DEFAULT = @MATCH_FG_DEFAULT@
MKDIR_P = @MKDIR_P@
MSGFMT = @MSGFMT@
MSGFMT_015 = @MSGFMT_015@
MSGMERGE = @MSGMERGE@
NAMES_DEFAULT_URI = @NAMES_DEFAULT_URI@
NCURSES_LIBS = @NCURSES_LIBS@
OBJEXT = @OBJEXT@
OMF_DIR = @OMF_DIR@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PERL = @PERL@
PKG_CONFIG = @PKG_CONFIG@
PKG_CONFIG_LIBDIR = @PKG_CONFIG_LIBDIR@
PKG_CONFIG_PATH = @PKG_CONFIG_PATH@
POSUB = @POSUB@
RADICALS_DEFAULT_URI = @RADICALS_DEFAULT_URI@
RELEASE = @RELEASE@
RPMBUILD = @RPMBUILD@
RPMDIR = @RPMDIR@
RSYNC = @RSYNC@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SPANISH_DEFAULT_URI = @SPANISH_DEFAULT_URI@
STRIP = @STRIP@
UNZIP = @UNZIP@
USE_NLS = @USE_NLS@
VERSION = @VERSION@
WIN32_CFLAGS = @WIN32_CFLAGS@
WIN32_LIBS = @WIN32_LIBS@
XGETTEXT = @XGETTEXT@
XGETTEXT_015 = @XGETTEXT_015@
XGETTEXT_EXTRA_OPTIONS = @XGETTEXT_EXTRA_OPTIONS@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_CC = @ac_ct_CC@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@

#Install Application icon
data16dir = $(datadir)/icons/hicolor/16x16/apps
data24dir = $(datadir)/icons/hicolor/24x24/apps
data32dir = $(datadir)/icons/hicolor/32x32/apps
data48dir = $(datadir)/icons/hicolor/48x48/apps
data16_DATA = 16x16/$(PACKAGE).png
data24_DATA = 24x24/$(PACKAGE).png
data32_DATA = 32x32/$(PACKAGE).png
data48_DATA = 48x48/$(PACKAGE).png

#Install Application Logo
pkgdata_DATA = logo.png goo.png google.png wikipedia.png book_green.png

#Install Toolbar/Menuitem Icons
datawordboundary16dir = $(datadir)/icons/hicolor/16x16/actions
datawordboundary22dir = $(datadir)/icons/hicolor/22x22/actions
datawordboundary24dir = $(datadir)/icons/hicolor/24x24/actions
datanonwordboundary16dir = $(datadir)/icons/hicolor/16x16/actions
datanonwordboundary22dir = $(datadir)/icons/hicolor/22x22/actions
datanonwordboundary24dir = $(datadir)/icons/hicolor/24x24/actions
dataunknowncharacter16dir = $(datadir)/icons/hicolor/16x16/actions
dataunknowncharacter22dir = $(datadir)/icons/hicolor/22x22/actions
dataunknowncharacter24dir = $(datadir)/icons/hicolor/24x24/actions
datawordboundary16_DATA = 16x16/word-boundary.png
datawordboundary22_DATA = 22x22/word-boundary.png
datawordboundary24_DATA = 24x24/word-boundary.png
datanonwordboundary16_DATA = 16x16/non-word-boundary.png
datanonwordboundary22_DATA = 22x22/non-word-boundary.png
datanonwordboundary24_DATA = 24x24/non-word-boundary.png
dataunknowncharacter16_DATA = 16x16/unknown-character.png
dataunknowncharacter22_DATA = 22x22/unknown-character.png
dataunknowncharacter24_DATA = 24x24/unknown-character.png
EXTRA_DIST = logo.png 16x16/$(PACKAGE).png 24x24/$(PACKAGE).png 32x32/$(PACKAGE).png 48x48/$(PACKAGE).png 22x22/word-boundary.png 16x16/non-word-boundary.png 16x16/unknown-character.png 24x24/non-word-boundary.png 16x16/word-boundary.png 24x24/unknown-character.png 22x22/non-word-boundary.png 24x24/word-boundary.png 22x22/unknown-character.png goo.png google.png wikipedia.png book_green.png
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu src/images/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --gnu src/images/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):
install-data16DATA: $(data16_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(data16dir)" || $(MKDIR_P) "$(DESTDIR)$(data16dir)"
	@list='$(data16_DATA)'; test -n "$(data16dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(data16dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(data16dir)" || exit $$?; \
	done

uninstall-data16DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(data16_DATA)'; test -n "$(data16dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(data16dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(data16dir)" && rm -f $$files
install-data24DATA: $(data24_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(data24dir)" || $(MKDIR_P) "$(DESTDIR)$(data24dir)"
	@list='$(data24_DATA)'; test -n "$(data24dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(data24dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(data24dir)" || exit $$?; \
	done

uninstall-data24DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(data24_DATA)'; test -n "$(data24dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(data24dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(data24dir)" && rm -f $$files
install-data32DATA: $(data32_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(data32dir)" || $(MKDIR_P) "$(DESTDIR)$(data32dir)"
	@list='$(data32_DATA)'; test -n "$(data32dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(data32dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(data32dir)" || exit $$?; \
	done

uninstall-data32DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(data32_DATA)'; test -n "$(data32dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(data32dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(data32dir)" && rm -f $$files
install-data48DATA: $(data48_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(data48dir)" || $(MKDIR_P) "$(DESTDIR)$(data48dir)"
	@list='$(data48_DATA)'; test -n "$(data48dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(data48dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(data48dir)" || exit $$?; \
	done

uninstall-data48DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(data48_DATA)'; test -n "$(data48dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(data48dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(data48dir)" && rm -f $$files
install-datanonwordboundary16DATA: $(datanonwordboundary16_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(datanonwordboundary16dir)" || $(MKDIR_P) "$(DESTDIR)$(datanonwordboundary16dir)"
	@list='$(datanonwordboundary16_DATA)'; test -n "$(datanonwordboundary16dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(datanonwordboundary16dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(datanonwordboundary16dir)" || exit $$?; \
	done

uninstall-datanonwordboundary16DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(datanonwordboundary16_DATA)'; test -n "$(datanonwordboundary16dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(datanonwordboundary16dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(datanonwordboundary16dir)" && rm -f $$files
install-datanonwordboundary22DATA: $(datanonwordboundary22_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(datanonwordboundary22dir)" || $(MKDIR_P) "$(DESTDIR)$(datanonwordboundary22dir)"
	@list='$(datanonwordboundary22_DATA)'; test -n "$(datanonwordboundary22dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(datanonwordboundary22dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(datanonwordboundary22dir)" || exit $$?; \
	done

uninstall-datanonwordboundary22DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(datanonwordboundary22_DATA)'; test -n "$(datanonwordboundary22dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(datanonwordboundary22dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(datanonwordboundary22dir)" && rm -f $$files
install-datanonwordboundary24DATA: $(datanonwordboundary24_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(datanonwordboundary24dir)" || $(MKDIR_P) "$(DESTDIR)$(datanonwordboundary24dir)"
	@list='$(datanonwordboundary24_DATA)'; test -n "$(datanonwordboundary24dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(datanonwordboundary24dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(datanonwordboundary24dir)" || exit $$?; \
	done

uninstall-datanonwordboundary24DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(datanonwordboundary24_DATA)'; test -n "$(datanonwordboundary24dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(datanonwordboundary24dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(datanonwordboundary24dir)" && rm -f $$files
install-dataunknowncharacter16DATA: $(dataunknowncharacter16_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(dataunknowncharacter16dir)" || $(MKDIR_P) "$(DESTDIR)$(dataunknowncharacter16dir)"
	@list='$(dataunknowncharacter16_DATA)'; test -n "$(dataunknowncharacter16dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dataunknowncharacter16dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dataunknowncharacter16dir)" || exit $$?; \
	done

uninstall-dataunknowncharacter16DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dataunknowncharacter16_DATA)'; test -n "$(dataunknowncharacter16dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(dataunknowncharacter16dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(dataunknowncharacter16dir)" && rm -f $$files
install-dataunknowncharacter22DATA: $(dataunknowncharacter22_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(dataunknowncharacter22dir)" || $(MKDIR_P) "$(DESTDIR)$(dataunknowncharacter22dir)"
	@list='$(dataunknowncharacter22_DATA)'; test -n "$(dataunknowncharacter22dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dataunknowncharacter22dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dataunknowncharacter22dir)" || exit $$?; \
	done

uninstall-dataunknowncharacter22DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dataunknowncharacter22_DATA)'; test -n "$(dataunknowncharacter22dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(dataunknowncharacter22dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(dataunknowncharacter22dir)" && rm -f $$files
install-dataunknowncharacter24DATA: $(dataunknowncharacter24_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(dataunknowncharacter24dir)" || $(MKDIR_P) "$(DESTDIR)$(dataunknowncharacter24dir)"
	@list='$(dataunknowncharacter24_DATA)'; test -n "$(dataunknowncharacter24dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dataunknowncharacter24dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dataunknowncharacter24dir)" || exit $$?; \
	done

uninstall-dataunknowncharacter24DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dataunknowncharacter24_DATA)'; test -n "$(dataunknowncharacter24dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(dataunknowncharacter24dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(dataunknowncharacter24dir)" && rm -f $$files
install-datawordboundary16DATA: $(datawordboundary16_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(datawordboundary16dir)" || $(MKDIR_P) "$(DESTDIR)$(datawordboundary16dir)"
	@list='$(datawordboundary16_DATA)'; test -n "$(datawordboundary16dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(datawordboundary16dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(datawordboundary16dir)" || exit $$?; \
	done

uninstall-datawordboundary16DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(datawordboundary16_DATA)'; test -n "$(datawordboundary16dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(datawordboundary16dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(datawordboundary16dir)" && rm -f $$files
install-datawordboundary22DATA: $(datawordboundary22_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(datawordboundary22dir)" || $(MKDIR_P) "$(DESTDIR)$(datawordboundary22dir)"
	@list='$(datawordboundary22_DATA)'; test -n "$(datawordboundary22dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(datawordboundary22dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(datawordboundary22dir)" || exit $$?; \
	done

uninstall-datawordboundary22DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(datawordboundary22_DATA)'; test -n "$(datawordboundary22dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(datawordboundary22dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(datawordboundary22dir)" && rm -f $$files
install-datawordboundary24DATA: $(datawordboundary24_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(datawordboundary24dir)" || $(MKDIR_P) "$(DESTDIR)$(datawordboundary24dir)"
	@list='$(datawordboundary24_DATA)'; test -n "$(datawordboundary24dir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(datawordboundary24dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(datawordboundary24dir)" || exit $$?; \
	done

uninstall-datawordboundary24DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(datawordboundary24_DATA)'; test -n "$(datawordboundary24dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(datawordboundary24dir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(datawordboundary24dir)" && rm -f $$files
install-pkgdataDATA: $(pkgdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(pkgdatadir)" || $(MKDIR_P) "$(DESTDIR)$(pkgdatadir)"
	@list='$(pkgdata_DATA)'; test -n "$(pkgdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(pkgdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(pkgdatadir)" || exit $$?; \
	done

uninstall-pkgdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(pkgdata_DATA)'; test -n "$(pkgdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(pkgdatadir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(pkgdatadir)" && rm -f $$files
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(data16dir)" "$(DESTDIR)$(data24dir)" "$(DESTDIR)$(data32dir)" "$(DESTDIR)$(data48dir)" "$(DESTDIR)$(datanonwordboundary16dir)" "$(DESTDIR)$(datanonwordboundary22dir)" "$(DESTDIR)$(datanonwordboundary24dir)" "$(DESTDIR)$(dataunknowncharacter16dir)" "$(DESTDIR)$(dataunknowncharacter22dir)" "$(DESTDIR)$(dataunknowncharacter24dir)" "$(DESTDIR)$(datawordboundary16dir)" "$(DESTDIR)$(datawordboundary22dir)" "$(DESTDIR)$(datawordboundary24dir)" "$(DESTDIR)$(pkgdatadir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-data16DATA install-data24DATA \
	install-data32DATA install-data48DATA \
	install-datanonwordboundary16DATA \
	install-datanonwordboundary22DATA \
	install-datanonwordboundary24DATA \
	install-dataunknowncharacter16DATA \
	install-dataunknowncharacter22DATA \
	install-dataunknowncharacter24DATA \
	install-datawordboundary16DATA install-datawordboundary22DATA \
	install-datawordboundary24DATA install-pkgdataDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook
install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-data16DATA uninstall-data24DATA \
	uninstall-data32DATA uninstall-data48DATA \
	uninstall-datanonwordboundary16DATA \
	uninstall-datanonwordboundary22DATA \
	uninstall-datanonwordboundary24DATA \
	uninstall-dataunknowncharacter16DATA \
	uninstall-dataunknowncharacter22DATA \
	uninstall-dataunknowncharacter24DATA \
	uninstall-datawordboundary16DATA \
	uninstall-datawordboundary22DATA \
	uninstall-datawordboundary24DATA uninstall-pkgdataDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) uninstall-hook
.MAKE: install-am install-data-am install-strip uninstall-am

.PHONY: all all-am check check-am clean clean-generic distclean \
	distclean-generic distdir dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am \
	install-data-hook install-data16DATA install-data24DATA \
	install-data32DATA install-data48DATA \
	install-datanonwordboundary16DATA \
	install-datanonwordboundary22DATA \
	install-datanonwordboundary24DATA \
	install-dataunknowncharacter16DATA \
	install-dataunknowncharacter22DATA \
	install-dataunknowncharacter24DATA \
	install-datawordboundary16DATA install-datawordboundary22DATA \
	install-datawordboundary24DATA install-dvi install-dvi-am \
	install-exec install-exec-am install-html install-html-am \
	install-info install-info-am install-man install-pdf \
	install-pdf-am install-pkgdataDATA install-ps install-ps-am \
	install-strip installcheck installcheck-am installdirs \
	maintainer-clean maintainer-clean-generic mostlyclean \
	mostlyclean-generic pdf pdf-am ps ps-am uninstall uninstall-am \
	uninstall-data16DATA uninstall-data24DATA uninstall-data32DATA \
	uninstall-data48DATA uninstall-datanonwordboundary16DATA \
	uninstall-datanonwordboundary22DATA \
	uninstall-datanonwordboundary24DATA \
	uninstall-dataunknowncharacter16DATA \
	uninstall-dataunknowncharacter22DATA \
	uninstall-dataunknowncharacter24DATA \
	uninstall-datawordboundary16DATA \
	uninstall-datawordboundary22DATA \
	uninstall-datawordboundary24DATA uninstall-hook \
	uninstall-pkgdataDATA


install-data-hook: update-icon-cache
uninstall-hook: update-icon-cache

@GCONF_SCHEMAS_INSTALL_TRUE@update-icon-cache:
@GCONF_SCHEMAS_INSTALL_TRUE@	gtk-update-icon-cache $(datadir)/icons/hicolor || true
@GCONF_SCHEMAS_INSTALL_FALSE@update-icon-cache:

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
