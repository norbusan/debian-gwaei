## Process this file with automake to produce Makefile.in

PACKAGE = @PACKAGE@
VERSION = @VERSION@

datadesktopdir = $(prefix)/share/applications
datadesktop_DATA = $(PACKAGE).desktop

EXTRA_DIST = $(PACKAGE).desktop

